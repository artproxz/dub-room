import { spawn } from 'node:child_process';import { mkdtemp,rm } from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import assert from 'node:assert/strict';
const PORT=3312,base=`http://127.0.0.1:${PORT}`,data=await mkdtemp(path.join(os.tmpdir(),'dubroom-v12-'));const child=spawn(process.execPath,['server/index-v5.js'],{cwd:process.cwd(),env:{...process.env,PORT:String(PORT),DATA_DIR:data},stdio:['ignore','pipe','pipe']});
async function wait(){for(let i=0;i<80;i++){try{const r=await fetch(`${base}/api/health`);if(r.ok)return;}catch{}await new Promise(r=>setTimeout(r,40));}throw new Error('server');}
async function j(url,opt={}){const r=await fetch(url,{...opt,headers:{'content-type':'application/json',...(opt.headers||{})}});const b=await r.json().catch(()=>({}));return{r,b};}
try{await wait();let q=await j(`${base}/api/rooms`,{method:'POST',body:JSON.stringify({participantId:'p1',name:'One'})});const id=q.b.room.id;for(const [pid,name] of [['p2','Two'],['p3','Three']])await j(`${base}/api/rooms/${id}/join`,{method:'POST',body:JSON.stringify({participantId:pid,name})});let r=await fetch(`${base}/api/rooms/${id}/video`,{method:'POST',headers:{'content-type':'video/mp4','x-participant-id':'p1','x-file-name':'movie.mp4'},body:Buffer.from('0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ')});assert.equal(r.status,200);await j(`${base}/api/rooms/${id}/video-meta`,{method:'POST',body:JSON.stringify({participantId:'p1',duration:60})});await j(`${base}/api/rooms/${id}/range`,{method:'POST',body:JSON.stringify({participantId:'p1',start:10,end:25})});q=await j(`${base}/api/rooms/${id}/party/action`,{method:'POST',body:JSON.stringify({participantId:'p1',action:'scene-ready'})});assert.equal(q.r.status,200);assert.equal(q.b.party.phase,'roles');q=await j(`${base}/api/rooms/${id}/party/action`,{method:'POST',body:JSON.stringify({participantId:'p1',action:'edit-scene'})});assert.equal(q.r.status,200);assert.equal(q.b.party.phase,'lobby');q=await j(`${base}/api/rooms/${id}/party/action`,{method:'POST',body:JSON.stringify({participantId:'p2',action:'scene-ready'})});assert.equal(q.r.status,403);q=await j(`${base}/api/rooms/${id}/party/action`,{method:'POST',body:JSON.stringify({participantId:'p1',action:'scene-ready'})});assert.equal(q.b.party.phase,'roles');for(const pid of ['p1','p2','p3'])await j(`${base}/api/rooms/${id}/participant`,{method:'POST',body:JSON.stringify({participantId:pid,ready:true})});q=await j(`${base}/api/rooms/${id}/party/start`,{method:'POST',body:JSON.stringify({participantId:'p1'})});assert.equal(q.r.status,200);assert.equal(q.b.party.phase,'recording');console.log('✓ DubRoom 1.2 scene → roles → recording flow passed');}finally{child.kill('SIGTERM');await rm(data,{recursive:true,force:true});}
